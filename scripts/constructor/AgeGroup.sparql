PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

CONSTRUCT {
    GRAPH ?g {
         ?agegroup_translated  rdf:type ?typeURI .
     }
} WHERE { 
 ?user <http://example.org/_/age> ?agegroup .
 FILTER CONTAINS(STR(?user),"User") .
 
 BIND(URI(CONCAT("http://example.org/", "federated_shop")) AS ?g) .
 BIND(URI(REPLACE(STR(?agegroup), "_/", "federated_shop/")) AS ?agegroup_translated) .

 BIND(REPLACE(STR(?agegroup), STR("http://example.org/_/"),"") AS ?type1) .
 BIND(REPLACE(STR(?type1), STR("_[0-9]+"),"") AS ?type) .
 BIND(URI(CONCAT("http://example.org/federated_shop/", ?type)) AS ?typeURI) .

}